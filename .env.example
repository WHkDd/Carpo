# 主机侧发布端口。接口没有任何鉴权，除非发布到可信网络，
# 建议写成 127.0.0.1:8787 只在本机可访问。
CARPO_HOST_PORT=127.0.0.1:8787

# 服务端模式下，模型 Base URL 必须是 https 且不得指向环回/内网地址。
# 如果你的模型服务本来就跑在内网，把它的主机名列在这里豁免，逗号分隔，
# 支持 `host` 或 `host:port`。留空表示不豁免任何地址。
# CARPO_BASE_URL_ALLOWLIST=ollama:11434,vllm.internal
