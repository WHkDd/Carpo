# Changelog

## [0.1.1] — 2026-05-21

### 新增功能

#### Paddle 文档级全文 OCR
- 新增文档级整文件 OCR 模式，一次性对整份 PDF 调用 Paddle 批量接口，无需逐页手动触发。
- 支持将大文件自动分块（chunked）分批提交，规避 Paddle API 单次上传限制。
- 新增 `PaddleDocumentOptions` 可配置项：可调节每批页数、语言、并发数等参数，满足不同文件规格的识别需求。

#### Paddle JSON 导入增强
- 新增 **导入预检（preflight）**：导入前校验 JSON 区块结构完整性，提前发现格式异常，避免无效写入。
- 支持从 Paddle 网页版导出的 JSON **重建版式 PDF**：按原始 bbox 坐标将识别文字回贴到对应位置，生成可复制文字的版式 PDF。
- 简化导入对话框 UI：移除导入目标选择器，交互流程更直接。

#### PDF 导航与识别范围控制
- 新增页码输入跳转：PDF 查看器支持直接输入页码快速定位，无需逐页翻动。
- 全文识别支持非连续页码范围语法，例如 `1-5,8,10-12`，精确控制识别页集合。

### 性能优化
- 预检 OCR 渲染开销大幅降低：优化渲染路径，减少不必要的重绘，大文件预检更流畅。

### 内部改进
- 引入规范化的 `RecognizedPages` store，统一管理各页识别结果，修复若干边界条件下的状态不一致问题。
- 移除 macOS Intel（x86_64）构建目标，专注 Apple Silicon。
- Rust 测试脚本自包含化，无需额外环境准备即可独立运行。

---

## [0.1.0] — 2026-05-14

首次公开发布。核心功能：框选报道 OCR、全文按页识别、多 provider 路由（PaddleOCR / OpenAI Vision / OpenRouter / 自建端点）、Paddle JSON 基础导入、结果导出为 Markdown / 纯文本、本地凭据管理、细粒度取消、PDFium 专用线程渲染。
